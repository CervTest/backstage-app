# Example Domain: DevOps

These files contain entities to show a typical DevOps "department" organized in Backstage.

## Further musings

* Environments are modeled as sub-components yet are still plain Components - should they perhaps be resources? But that's typically an explicit thing, envs are at least in some sense abstract still ...
